IPC -> jtfg-sp-components extraction TODO

Phase 1 (move now)
- [x] Move UniversalGrid as core reusable primitive (already started, continue hardening).
- [x] Add ConfirmDialog component.
- [x] Add ImportMapper component.
- [x] Export interfaces/types for moved components.
- [x] Add minimal usage examples to README.
- [x] Validate with npm run typecheck && npm run build.

Phase 2 (small abstractions, then move)
- [x] Add FormLayout with configurable labels/actions (no IPC text hardcoded).
- [x] Extract PricelistPicker into generic HierarchyPicker with data-provider input.
- [ ] Extract EntityCreateDialog into generic CreateEntityDialog with injected fields/options.
- [ ] Add shared contracts:
  - [ ] TreeNode
  - [ ] ImportSource / ColumnMapping
- [ ] Add optional reusable hooks:
  - [ ] useGridStatePersistence
  - [ ] useHierarchyBrowser
  - [ ] useSpreadsheetImportMapper

Phase 3 (keep domain in IPC, consume package)
- [ ] Keep domain-specific components in IPC:
  - ApprovalActions
  - DocumentsTab
  - ListView
  - PricelistDetail
  - ExceptionDetail
  - IPC root orchestration component
- [ ] Replace IPC local primitives with package imports incrementally.
- [ ] Add boundary tests around SharePoint/service adapters in IPC.

Notes
- Target package is SPFx-only, so browser storage usage is acceptable.
- Prioritize reusable UI primitives first, domain workflows later.

Rescan Suggestions (2026-05-12)
- [ ] Remove stale local duplicates in IPC that are now package-owned (commented code files):
  - components/HierarchyPicker.tsx
  - views/ExplorerLayout.tsx
  - views/ViewTabs.tsx
  - views/EntityActionToolbar.tsx
- [ ] Keep only one FolderTree source of truth:
  - decide whether to keep IPC-local FolderTree or move FolderTree into package and consume it from there
- [ ] Convert EntityCreateDialog to package-level CreateEntityDialog API:
  - keep IPC-specific service/data mapping in wrapper
  - pass normalized tree nodes and callbacks into reusable dialog
- [ ] Add contracts for import pipeline in package:
  - ImportSource
  - ColumnMapping
- [ ] Add small migration validation checklist in IPC:
  - ListView compiles without local ExplorerLayout/ViewTabs/EntityActionToolbar imports
  - PricelistPicker and EntityCreateDialog both use package HierarchyPicker
  - npm run build passes in IPC after duplicate file removal
